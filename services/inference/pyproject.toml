[project]
name = "rfpose-inference"
version = "0.2.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "onnxruntime",
    "numpy",
    "pydantic",
    "nats-py",
]

[tool.setuptools]
packages = ["rfpose_inference"]
package-dir = {"" = "src"}
