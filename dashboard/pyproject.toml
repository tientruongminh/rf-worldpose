[project]
name = "rfpose-portal"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "jinja2",
    "pydantic",
    "pydantic-settings",
    "psycopg[binary]",
    "python-multipart",
]
